services:
  - type: web
    name: licitaciones-backend
    env: python
    plan: free
    pythonVersion: 3.11
    buildCommand: cd backend && + pip install -r requirements.txt 
    startCommand: cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000